#![doc(hidden)]

// this crate is simply a stub to get the `procedural-fork` tests to pass since they expect a
// valid `Cargo.toml` at this path with `frame-system` and `frame-support` as dependencies.
