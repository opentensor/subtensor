"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.pushDeep = exports.mapToSet = void 0;
/**
 * @internal
 */
function mapToSet(map, { mapper, filter }) {
    const set = new Set();
    map.forEach((value, key) => {
        if (!filter || (filter && filter(value, key))) {
            const item = mapper(value, key);
            set.add(item);
        }
    });
    return set;
}
exports.mapToSet = mapToSet;
/**
 * Manages creating and pushing to sub-arrays.
 * @internal
 */
function pushDeep(arrayOfArrays, index, item) {
    if (arrayOfArrays[index]) {
        arrayOfArrays[index].push(item);
    }
    else {
        arrayOfArrays[index] = [item];
    }
}
exports.pushDeep = pushDeep;
