import { Blake2256, Keccak256 } from '@polkadot-api/substrate-bindings';
import { toHex, fromHex } from '@polkadot-api/utils';

const hashers = [Blake2256, Keccak256];
const getHasherFromHeader = (header, blockHash) => hashers.find((h) => toHex(h(fromHex(header))) === blockHash) || (() => {
  throw new Error("Unsupported hasher");
});

export { getHasherFromHeader };
//# sourceMappingURL=hashers.mjs.map
