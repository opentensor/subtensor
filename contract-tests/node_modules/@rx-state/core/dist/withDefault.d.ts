import { Observable, UnaryFunction } from "rxjs";
import { DefaultedStateObservable } from "./StateObservable";
export interface WithDefaultOperator<T, R> extends UnaryFunction<Observable<T>, DefaultedStateObservable<T | R>> {
}
export declare function withDefault<T, D>(defaultValue: D): WithDefaultOperator<T, D>;
