import { Observable } from "rxjs";
export declare class Effect<T> {
    readonly value: T;
    constructor(value: T);
}
export declare const effect: <T>(value: T) => Effect<T>;
export declare const mapEffect: <Input, Output>(mapper: (input: Input) => Output) => (source$: Observable<Input>) => Observable<Exclude<Output, Effect<any>>>;
export declare const liftEffects: <E>() => <T>(source$: Observable<T>) => Observable<E | T>;
