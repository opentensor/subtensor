[workspace]
members = [
    "integration-tests",
    "node",
    "pallets/subtensor",
    "pallets/commitments",
    "runtime",
    "spec-version",
]

[profile.release]
panic = "unwind"

[profile.test]
opt-level = 3
