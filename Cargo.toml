[workspace]
members = [
    "integration-tests",
    "node",
    "pallets/subtensor",
    "runtime",
]

[profile.ci-dev]
inherits = "dev"
incremental = false

[profile.ci-test]
inherits = "ci-dev"

[profile.ci-release]
inherits = "release"
panic = "unwind"
opt-level = 3
codegen-units = 8
