[workspace]
members = [
    "integration-tests",
    "node",
    "pallets/subtensor",
    "pallets/commitments",
    "runtime",
]
resolver = "2"

[profile.release]
panic = "unwind"

[profile.test]
opt-level = 3
