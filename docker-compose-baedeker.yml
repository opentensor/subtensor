services:
  localnet:
    ports:
      - 9946:9946
    volumes:
      - ./target:/substrate
    build:
      context: .
      dockerfile: ./.baedeker/Dockerfile
    restart: "unless-stopped"
    network_mode: "bridge"