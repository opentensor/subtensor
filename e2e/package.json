{
  "name": "e2e",
  "private": true,
  "scripts": {
    "build-node": "cargo build --manifest-path ../Cargo.toml --profile release -p node-subtensor",
    "generate-types": "pnpm dlx polkadot-api add subtensor --wsUrl ws://localhost:9944 --skip-codegen && pnpm dlx polkadot-api",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@polkadot-api/descriptors": "file:.papi/descriptors",
    "polkadot-api": "catalog:"
  },
  "devDependencies": {
    "prettier": "catalog:"
  }
}
