{
  "name": "e2e",
  "private": true,
  "scripts": {
    "build-node": "cargo build --manifest-path ../Cargo.toml --profile release -p node-subtensor",
    "generate-types": "papi add subtensor --wsUrl ws://localhost:9944 --skip-codegen && papi",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@polkadot-api/descriptors": "file:.papi/descriptors",
    "polkadot-api": "catalog:"
  },
  "devDependencies": {
    "prettier": "catalog:"
  }
}
