{
  "name": "e2e",
  "private": true,
  "scripts": {
    "build-node": "cargo build --manifest-path ../Cargo.toml --profile release -p node-subtensor",
    "generate-types": "pnpm --package=@dedot/cli dlx dedot chaintypes -w ws://localhost:9944",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "dedot": "catalog:",
    "prettier": "catalog:"
  }
}
